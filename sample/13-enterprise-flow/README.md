# 13-enterprise-flow

Enterprise onboarding sample for Navigation v2 coverage.

- Uses `version: "2"` graph-first metadata.
- Demonstrates branch, backtrack, retry, escalation, and loop edges.
- Marks `launch` as a terminal success screen and opts into `policy.loops: allow`.
